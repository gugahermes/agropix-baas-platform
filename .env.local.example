# Integração real com a Focus NFe (opcional — sem isso, a emissão de NF-e
# roda em modo simulado, com o mesmo formato de resposta da API real).
# Token gerado gratuitamente no painel da Focus NFe, ambiente de homologação.
# NUNCA commitar o token real aqui — esse arquivo é só o exemplo, o .env.local
# de verdade já está no .gitignore (padrão *.local).
VITE_FOCUS_NFE_TOKEN=
VITE_FOCUS_NFE_ENV=homologacao
